# 2026-08-14 日清夜跑协商结果

记录时间：2026-08-14 17:03 后  
状态：用户已选择唯一目标

## 用户选择的唯一目标

今晚夜跑“全跑”飞书远程控制 Codex 剩余可靠性收口与受控真实链路冒烟：

1. 防止 `storage.dataDir` 配置进受管仓库，避免运行文件污染项目工作区。
2. 修复无进度超时可被无效 stdout/stderr 噪声续命的问题，并让 worktree 创建失败的 Git 上下文能回传给移动端/飞书用户。
3. 在前两项本地测试通过后，以只读任务跑一次受控真实“飞书 -> 本地桥接 -> Codex -> 回传”端到端冒烟验证。

## 用户原话摘要

用户说：“全跑”。

## 选择理由与证据

- 共享任务表 `T-019` 的三项 P1 已在上一轮完成本地修复，`49/49` 本地测试、doctor、simulate 和 `git diff --check` 均通过。
- 最新验收摘要仍为 `PARTIAL`：剩余已证实风险为运行目录污染、无进度超时语义和 worktree 失败上下文回传；真实飞书到 Codex 的端到端链路仍未验证。
- 用户选择“全跑”，因此将两项本地 P2 可靠性收口和一项受控真实链路验证作为同一个有序目标：本地证据先行，真实验证后置。

## 明确非目标

- 本协商任务不执行代码修改、测试、构建、Git 提交/推送、部署或飞书操作。
- 真实链路验证不运行写入、提交、推送、部署、删除或迁移命令，不读取私聊、日历、生产业务数据或真实用户数据。
- 不处理新扩展板实板验收；该人工任务 `T-022` 仍需单独负责人、设备和绝对截止时间。

## 依赖与风险

- 真实冒烟仅在本地可靠性测试通过后进行，且依赖可用的受控群、授权身份、本地桥接和 Codex 环境。
- 真实飞书或 Codex 任一侧不可用、权限不匹配、需要生产服务或超出只读边界时，立即停止真实验证分支并如实标记 N/A；本地 P2 成果仍按实际结果记录。
- 若 P2 修复扩大到远程应用变更、Git 写操作、部署或生产服务访问，停止扩大范围并回报。

## 建议验收方式

- 为目录守卫、无进度超时和 worktree 失败上下文分别补充定向测试。
- 复跑全量 Node 测试、doctor、simulate 与 `git diff --check`，记录真实输出。
- 真实验证只发送预先约束的只读状态查询，核对飞书接收、队列状态、Codex 执行与回执闭环；记录发送身份、会话、时间、消息 ID 和脱敏后的结果。
- 严格按证据给出 `PASS`、`PARTIAL` 或 `FAIL`，不因单项通过把真实链路写成已完全验收。

## 交接

本文件仅供“日清夜跑17点”计划任务读取后生成当天 `plan.md` 并提交飞书计划表。
