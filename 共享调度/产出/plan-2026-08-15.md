# 2026-08-15 日清夜跑计划

## 唯一目标

完成 `bean_ye_2` 新扩展板低功耗验收的**离线证据一致性审计与人工测量交接包**：把已通过的实机功能证据、尚未闭环的自然深睡 TF 断电复测和整机深睡电流基线，整理成一份可由现场人工执行、可复核、不会伪造实测结论的最小验收包。

本轮只准备和审计验收条件；不操作真实板卡。

## 背景与证据

- 当天协商结果文件 `/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/协商结果-2026-08-15.md` 不存在，未读取到用户确认的唯一目标；因此按共享任务表、上轮修改记录和项目当前文档选择证据最充分的未闭环事项。
- 项目路线图将“新扩展板到板验收”标为进行中，已通过录音、上传、转写、断电恢复、App 断连文件列表、BLE 停播与 Reset 恢复；未闭环项为自然深睡 `VCC_SD_3V3` 正确测点复测和整机深睡电流基线。
- `docs/hardware/bean_ye_2_acceptance_20260814.md` 记录深睡时曾读到 `3.37V`，可能误测常供电 `3V3_IN`，要求改在 `C2` 非地端或 `L1` 的 TF 卡侧复测；整机深睡电流因缺少合适仪表和供电接法尚无可信数值。
- 共享任务表 `T-022` 仍为阻塞：现场设备、人工负责人和绝对截止时间尚未确认。该阻塞不能用软件构建、文档审计或历史结果替代。

## 范围

- 只读核对 `bean_ye_2` 验收记录、到板验收表、正式固件验收流程、低功耗对比表、当前路线图与 T-022 状态，列出相互一致、缺失或不可互证的证据。
- 输出人工测量交接包：正确测点、所需仪表与接法、前置状态、记录字段、通过/不通过条件、原始证据命名和恢复步骤。
- 静态核对固件/烧录包文档中的 GPIO40、TF 关断、自然深睡、Reset 恢复与正式固件版本的引用是否和验收表一致；不编译、不烧录、不读取密钥。
- 建立独立人工实板验收任务的事实性阻塞卡，明确其未指定负责人和绝对截止时间，等待用户/现场补充后才允许实际执行。

## 非目标

- 不给真实板上电，不烧录固件，不测量电压/电流，不操作麦克风、TF、BLE、Reset 或实机 App。
- 不把历史 `0.017V` 自检结果当作自然深睡 `VCC_SD_3V3` 复测通过，也不填写未实测的整机电流或续航数字。
- 不修改固件、App、后端、硬件设计、BOM、PCB、现有验收记录或生产配置。
- 不连接生产服务、不读取真实用户数据或私聊、不提交或推送业务项目 Git。

## 执行者分工

### 执行者1：验收记录一致性审计

- 只读核对 `docs/hardware/bean_ye_2_acceptance_20260814.md`、`docs/hardware/board_arrival_acceptance_record.md` 与 `docs/planning/development_roadmap.md`。
- 产出“已通过/待补测/不可判定”三栏证据矩阵；逐项引用来源，不补写实测数据。
- 回传状态、已读输入、实际证据、N/A/阻塞项、下一步和绝对截止时间；不直接写共享调度文件。

### 执行者2：低功耗测量前置条件审计

- 只读核对 `docs/hardware/production_firmware_acceptance_flow.md` 与 `docs/hardware/full_charge_runtime_comparison_20260811.md`。
- 固化自然深睡测量的正确节点、仪表/供电接法、等待条件、记录格式与不可接受的替代测点；明确没有合适仪表时只能 N/A。
- 回传状态、已读输入、实际证据、N/A/阻塞项、下一步和绝对截止时间；不直接写共享调度文件。

### 执行者3：固件与恢复步骤静态核对

- 只读核对正式固件验收流程、到板验收表和烧录包说明中的 `bean_ye_2` 身份、`GPIO40/SD_PWR_EN`、TF 关断、正式固件 `0.3.8` 与 Reset 恢复要求。
- 输出只包含文档一致性结论与待现场确认项；不执行 PlatformIO、烧录、串口、网络或设备命令。
- 回传状态、已读输入、实际证据、N/A/阻塞项、下一步和绝对截止时间；不直接写共享调度文件。

### 执行者4：独立质量门禁与交接包终审

- 汇总前三项回传，检查没有把静态资料、历史自检或单次功能体验写成实际低功耗验收 PASS。
- 复核人工任务是否包含设备、前置条件、测点、证据目录、通过/不通过规则与恢复步骤；缺人工负责人或绝对截止时间时只能裁定为阻塞/PARTIAL。
- 在自己的任务会话回传完整终审证据、N/A/阻塞项、下一步和绝对截止时间；共享调度文件由调度者统一落盘。

## 独立人工实板验收任务（阻塞，非本轮执行）

- 任务 ID：`HW-20260815-01`。
- 人工负责人：**未指定（阻塞）**。
- 绝对截止时间（北京时间）：**未提供（阻塞）**。
- 现场设备：`bean_ye_2` 原 XIAO 主板、新扩展板、测试 TF 卡、可验证 USB-C 线、万用表，以及 USB-C 电流表/功率计或具有限流与稳定读数能力的实验电源。
- 前置条件：已备份测试 TF 卡；正式固件为 `xiao_esp32s3_custom_board_release_038`；无 BLE 连接、录音、上传或待扫描任务；能访问正确测点 `C2` 非地端或 `L1` 的 TF 卡侧。
- 验收动作：进入自然深睡并确认 BLE 停播；测 `VCC_SD_3V3` 对 GND；记录整机深睡电流、仪表、量程、供电方式和接法；Reset 唤醒后确认 TF、BLE 与历史文件恢复。
- 通过条件：深睡 TF 电源在正确节点接近 `0V`；整机电流只记录稳定实测基线而不套用虚构阈值；Reset 恢复正常；原始读数、照片/串口日志和复测条件可追溯。
- 恢复/回滚：测量后按正式固件启动流程确认设备恢复；若异常、发热、反复复位、文件系统风险或测点不确定，立即停止，保存现象，不反复上电或格式化后伪称通过。
- 逾期处理：在负责人和绝对截止时间补齐前持续标记 `阻塞`；本轮计划最高为 `PARTIAL`，不得写成完整实板验收计划或硬件 PASS。

## 依赖顺序

1. 执行者1、2、3并行完成离线证据与文档审计。
2. 调度者收敛冲突、缺口和引用路径，形成单一人工交接包。
3. 执行者4独立审查交接包与阻塞条件。
4. 仅在用户/现场明确提供人工负责人、绝对截止时间和设备条件后，才由人工另行执行 `HW-20260815-01`；本轮到此停止。

## 约束

- 只读取已授权的项目目录、共享调度区、上一轮修改/回传和必要源码/文档状态。
- 所有结论必须区分“历史实测”“本轮静态核对”“待现场实测”；不把三者混写。
- 不读取私聊、日历、真实用户数据、生产服务、密钥或本机敏感配置。
- 执行者不得直接写 `plan.md`、任务表、回传目录或产出目录；由调度者统一落盘。

## 测试与核验

- 文档交叉核对：验收记录、到板验收表、正式固件流程、低功耗对比表、路线图和 T-022 的名称、测点、状态、版本和未完成边界一致。
- 路径/引用检查：`rg -n 'VCC_SD_3V3|GPIO40|SD_PWR_EN|bean_ye_2|0.3.8|Reset' docs/hardware docs/planning firmware/board_bringup_package`，仅检查引用，不执行设备命令。
- 质量检查：人工交接包包含仪表、接法、正确测点、前置状态、原始证据目录、恢复步骤和阻塞规则；任何未知信息保留“未提供”。

## 验收标准

- 输出一份可复核的证据矩阵，明确哪些功能已有用户确认、哪些测量仍缺原始值、哪些结论不能由现有材料推出。
- 人工测量任务能使现场人员在不猜测 GPIO、测点、供电方式或固件身份的情况下准备验收，但不冒充已执行。
- `HW-20260815-01` 的负责人和绝对截止时间缺失被明确标为阻塞，且未写入任何虚构的硬件 PASS、功耗、续航或完成时间。
- 最终结果只能为“离线交接包完成、实板验收阻塞/PARTIAL”；实际硬件 PASS 只能由后续人工证据判定。

## 停止条件

- 发现文档对硬件版本、测点、接法或验收状态存在无法以现有材料消解的矛盾时，停止形成通过结论，保留冲突并等待人工确认。
- 需要真实上电、烧录、串口、网络、设备控制、生产服务或敏感配置值时，立即停止该分支。
- 缺少人工负责人、绝对截止时间、合适仪表、测试板卡或安全供电条件时，停止实板验收并保持阻塞。

## 产出路径

- 当前计划：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/plan.md`
- 计划归档：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/产出/plan-2026-08-15.md`
- 预计交接包：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/产出/2026-08-15-bean_ye_2-低功耗验收交接包/`
- 内容来源：`/Users/jh-05/Documents/smart recording beam/docs/hardware/bean_ye_2_acceptance_20260814.md`、`/Users/jh-05/Documents/smart recording beam/docs/hardware/board_arrival_acceptance_record.md`、`/Users/jh-05/Documents/smart recording beam/docs/hardware/production_firmware_acceptance_flow.md`、`/Users/jh-05/Documents/smart recording beam/docs/hardware/full_charge_runtime_comparison_20260811.md`、`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/任务表.md`
