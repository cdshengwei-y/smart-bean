# 2026-08-17 日清夜跑计划

## 唯一目标

按 2026-08-17 16:40 协商结果，使用已核验的机器人身份，在明确的目标群中发送一次且仅一次：

> 你好，我叫贾维斯，有什么问题或行动请告诉我

本计划只覆盖该条受控群消息动作及其证据核验，不扩展到其他夜跑任务。

## 背景

- 当天协商结果已由用户明确选定唯一目标，文件为 `/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/协商结果-2026-08-17.md`。
- 当前用户身份已核验为叶聪宇（OpenID：`ou_ed7a5c34fd1444f1fcef0e8bb94a36f3`），管理群为 `oc_8601110c57c02c5522236d05862315b0`，用户在群成员列表中。
- 发送动作必须证明发送身份是机器人，且目标群、权限、正文和发送结果均可回读核验；不能以用户身份冒充机器人成功。
- 共享任务表的既有门禁证据已读取；本轮不把既有测试或历史发送写成今天已完成。

## 范围

- 核验可用机器人身份、目标群候选和发送权限。
- 在目标群明确且权限满足时，发送指定正文一次。
- 按消息 ID 回读并核验正文、发送身份、群 ID、发送时间。
- 将真实结果记录为 PASS、PARTIAL 或 FAIL；未执行的消息发送不能写成已完成。

## 非目标

- 不执行其他夜跑任务，不改代码，不做本地测试。
- 不发送到私聊、未知群或其他群，不重复发送，不修改历史消息。
- 不读取私聊、日历、真实用户数据或生产服务。
- 不提交或推送业务项目 Git，不部署、不烧录、不连接真实硬件。

## 执行者分工

### 执行者 1：身份、群与权限核验

- 读取已授权的飞书身份和管理群成员信息。
- 确认机器人身份、目标群 ID、机器人在群内且具备发消息权限。
- 不发送消息；若任一项不明确，回传 BLOCKED 及具体原因。

### 执行者 2：受控消息发送

- 仅在执行者 1 明确确认后，用机器人身份发送一次指定正文。
- 逐字保持正文，不增加前后缀，不改用用户身份。
- 回传发送状态、消息 ID、群 ID、发送时间；不直接写共享调度文件。

### 执行者 3：消息回读核验

- 按消息 ID 回读消息。
- 核对正文完全一致、发送者为机器人、群 ID 正确、时间在本轮执行窗口内。
- 发现重复、身份不符或正文偏差时回传 FAIL/阻塞，不掩盖异常。

### 执行者 4：独立质量门禁

- 审核前三项回传，确认只发送一次、无用户身份冒充、无错误群投递。
- 将真实状态判为 PASS、PARTIAL 或 FAIL，并记录 N/A、阻塞项、下一步和绝对截止时间。
- 不直接写共享调度文件、任务表或产出目录。

## 依赖顺序

1. 执行者 1 先完成机器人身份、目标群和权限核验。
2. 执行者 2 仅在核验通过后发送一次消息。
3. 执行者 3 按消息 ID 回读核验。
4. 执行者 4 汇总证据并给出终态。
5. 调度者统一落盘计划、归档和日清记录。

## 约束

- 消息正文必须完全等于“你好，我叫贾维斯，有什么问题或行动请告诉我”。
- 只能使用机器人身份发送；不能把用户身份发送结果写成机器人结果。
- 目标群必须由授权上下文明确确认；不能凭猜测选群。
- 不得重复发送；已有相同消息且无法证明本轮唯一发送时停止。
- 所有发送、回读、权限或网络异常如实记录；不得伪造消息 ID、群 ID、时间或成功状态。
- 白天已完成事实与今晚计划分开记录；本计划不把消息发送写成已完成。

## 测试与核验

- 发送前：核验当前用户身份、目标群成员、机器人身份、机器人群成员关系和发消息权限。
- 发送时：记录完整正文、机器人身份、目标群 ID 和发送时间。
- 发送后：按消息 ID 回读，核对正文、发送者类型、群 ID、时间戳和唯一性。
- 提交前：检查今日总结含白天真实事实；输入材料含 Git 链接、归档说明和自查状态；协调栏含未解决权限/网络阻塞及下一步。
- 不运行代码测试；不连接生产服务；不进行业务 Git 操作。

## 验收标准

- PASS：指定正文已由机器人在明确目标群发送一次，消息 ID、群 ID、发送身份和发送时间均可回读核验。
- PARTIAL：计划、身份或权限核验完成，但消息未发送或回读证据不完整；必须明确未完成边界。
- FAIL：错误身份、错误群、正文不一致、重复发送、权限不满足或出现无法解释的异常。

## 停止条件

- 无法明确机器人身份、目标群或发送权限时停止。
- 只能使用用户身份发送时停止，不能冒充机器人成功。
- 发现可能重复发送、错误群或正文偏差时停止。
- 出现私聊、真实用户数据、生产服务或其他超范围请求时停止。

## 产出路径

- 当前计划：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/plan.md`
- 计划归档：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/产出/plan-2026-08-17.md`
- 当天协商结果：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/协商结果-2026-08-17.md`
- 目标表格：`https://acn9omuig41v.feishu.cn/base/JB1abJgEDafVMYsJRpvcj2Gdnch?table=tblSCx7LZImh0nF9`
