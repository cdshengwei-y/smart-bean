# 2026-09-14 日清夜跑计划

> 当天 `协商结果-2026-09-14.md` 缺失，以下目标沿用最近一次已确认且未被否定的声呐下一阶段目标；不视为当天用户重新确认。

## 目标

增加温度/声速变化、噪声和多径鲁棒性回归；实现 STM32/UART 实时数据适配器和连续采样演示；完成真实硬件接入前的电源、引脚、换能器和安全验收包。

## 已知基线

2026-09-09 本地 V0.2 范围 PASS：30 m 误差由 `-14.928 mm` 修正到 `+0.266 mm`，五类模拟场景、`sonar-v02-csv-1` fixture 和基础图通过。真实 UART、ADC、供电、换能器、烧录和水下测试仍为 N/A。

## 执行与验收

先固定温度/声速、噪声和多径的模拟假设并生成可重复 fixture；保持既有 CSV 字段、单位、版本和状态枚举，实现连续本地 UART 行解析、标准 CSV 输出、坏行诊断和基础绘图；再整理硬件接入前检查包，列明资料来源、待确认项、上电/停机边界及证据位置。30 m 绝对误差须不超过 `0.005 m`，否则保留失败证据并标记 PARTIAL。

## 实板边界

本轮仅做本地模拟、协议适配和资料包，不接线、供电、烧录或读取真实 UART/ADC。人工负责人和北京时间绝对截止未指定，实板验收为 `BLOCKED / N/A`，不能以清单或本地演示替代实测。

## 依赖、测试与停止条件

- 执行者1：鲁棒性 fixture；执行者2：UART 连续行适配器；执行者3：硬件接入前验收包；执行者4：独立复跑与终审。
- 依赖顺序：先固定模拟假设，再实现适配器和回归，随后整理验收包，最后独立复核。
- 测试：温度/声速、噪声、多径及既有场景回归；UART 分片/粘包、坏行、未知版本/状态、no-echo；合同字段和图表检查。
- 停止：需要真实硬件/生产服务/未授权资料，或参数来源不明、结果退化、证据不可复现。

## 产出

证据目录：`共享调度/产出/2026-09-14-sonar-next-stage/`。产出包括鲁棒性 fixture、UART 适配器与诊断、CSV/图表、硬件接入前验收包和验收摘要；不修改业务项目源码或生产环境。
