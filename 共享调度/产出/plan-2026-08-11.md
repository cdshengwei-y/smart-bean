# 2026-08-11 日清夜跑计划

## 唯一目标

完成 `0.5.12` 后端到 Flutter 的 AI 处理状态与 Obsidian 同步状态链路本地离线验收，形成可复现的测试证据和未闭环风险清单。

## 背景与证据

- 2026-08-11 当日源码状态显示，`backend/app/database.py`、`backend/status_server.py`、`backend/ai_worker.py`、`backend/tests/test_obsidian_sync.py`、`flutter_app/lib/main.dart`、`flutter_app/lib/models.dart` 和 `flutter_app/test/models_test.dart` 均有更新；Flutter 当前版本为 `0.5.12+36`，本地存在 `flutter_app/dist/AI-Recording-Bean-0.5.12.apk`。
- 当前代码已包含 `ai_pending`、`optimizing`、`filtering`、`ai_failed`、`completed`、`note_synced`、AI 重试及进程中断恢复等状态/字段，需要验证数据库、状态 API 与 Flutter 展示之间的合同一致性。
- 2026-08-11 计划生成前已在本地运行相关基线：后端 `tests.test_obsidian_sync tests.test_status_server tests.test_ai_worker` 共 5 项通过；Flutter `test/models_test.dart` 共 10 项通过。该结果只证明已执行范围，不等同于全量验收完成。
- `bean_ye_2` 正式固件启动记录已显示设备身份、microSD、麦克风、PDM 右声道和 BLE 就绪为 PASS；但整机深睡电流、录音/上传电流与 USB Reset 恢复仍缺实测。本夜跑不以自动化代替人工硬件测量。

## 范围

- 追踪数据库状态迁移、AI worker、状态 API、Obsidian 同步标记和 Flutter 状态展示/重试入口的字段与行为合同。
- 只使用临时目录、合成记录、mock 存储和项目现有测试，补齐可复现的离线测试证据。
- 若发现确定缺陷，只允许做与该状态链直接相关的最小本地修正，并补对应回归测试。
- 汇总测试命令、结果、差异、风险与人工后续事项。

## 非目标

- 不连接生产服务、真实 MinIO、真实 Obsidian 仓库、真实用户数据、私聊或日历。
- 不烧录、不连接或操作真实硬件，不声称完成深睡电流、续航、录音试听或 Reset 实测。
- 不修改麦克风独立断电硬件方案，不新增 PCB/BOM 结论。
- 不提交 Git、不推送、不部署、不发布 APK。
- 不做与状态链无关的重构、依赖升级或 UI 改版。

## 执行者分工

### 执行者1：数据库与 AI worker 状态机

- 审计 `database.py` 与 `ai_worker.py` 的状态迁移、尝试次数、失败恢复和 `retry_ai` 前置条件。
- 使用临时 SQLite 和 mock 服务覆盖成功、AI 失败、进程中断、人工重试四条路径。
- 输出状态迁移表、实际测试命令和结果；不得连接外部服务。

### 执行者2：状态 API 与 Obsidian 同步合同

- 核对 `status_server.py`、`obsidian_sync` 表和现有同步测试，确认 `note_synced` 的时间语义及跨数据库回查逻辑。
- 以合成记录验证 API 返回字段、失败信息、AI 重试接口和同步前后状态。
- 输出 API 样例、边界条件和发现的问题；样例不得包含真实用户数据。

### 执行者3：Flutter 解析与界面状态映射

- 核对 `models.dart`、`main.dart` 与后端真实字段，验证所有 AI 阶段、错误文案、同步状态和重试入口可被正确解析/显示。
- 运行 `flutter analyze`、`flutter test`；若需补测，仅增加状态合同相关测试。
- 不安装 APK、不连接手机、不触发真实网络请求。

### 执行者4：独立复核与证据汇总

- 在执行者1至3完成后，复核修改范围、状态矩阵、测试覆盖和所有“通过”结论的证据。
- 复跑后端全量单元测试与 Flutter 全量测试；记录准确数量、耗时与失败原因。
- 生成最终验收记录和人工硬件后续清单；不得把 N/A 项改写为 PASS。

## 依赖顺序

1. 执行者1与执行者2可并行，先固定后端状态和 API 合同。
2. 执行者3以执行者2确认的真实 API 字段为准完成 Flutter 合同核验。
3. 若任一执行者发现缺陷，先提交最小补丁与定向测试结果，再由相关执行者交叉复核。
4. 执行者4等待前三份回传齐全后开始终审；调度者最后汇总，不提前宣告成功。

## 约束

- 只读取项目目录、共享任务表、上一轮修改记录、调度回传和当前源码；禁止扩展到私聊、日历、真实用户数据或生产服务。
- 所有运行数据必须是合成数据，数据库和文件写入必须位于临时目录或本计划产出目录。
- 保留当前用户已有修改，不覆盖与目标无关的文件。
- 任何指标、通过数和行为结论必须来自实际命令输出；不确定项写明 N/A 或待人工确认。
- 若修改源码，先记录目标文件哈希和修改理由，且只改状态链相关文件。

## 测试

- 后端定向：`env PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_obsidian_sync tests.test_status_server tests.test_ai_worker`。
- 后端全量：在 `backend/` 执行 `env PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -p 'test_*.py'`。
- Flutter 定向：在 `flutter_app/` 执行 `flutter test test/models_test.dart`。
- Flutter 全量与静态检查：`flutter test`、`flutter analyze`。
- 合成端到端：临时 SQLite + mock 存储，依次验证 `ai_pending -> optimizing/filtering -> completed -> note_synced`，并单独验证 `ai_failed -> retry -> ai_pending`。

## 验收标准

- 形成一份数据库、API、Flutter 三层字段和状态映射表，所有字段来自当前源码或实际 API 输出。
- 成功、失败、中断恢复、人工重试和 Obsidian 同步五类路径均有离线可复现证据。
- 后端与 Flutter 全量测试实际通过；若不通过，最终状态必须为 PARTIAL/FAIL，并附首个失败及复现命令。
- 不发生生产访问、真实数据读取、硬件操作、Git 提交、推送、部署或烧录。
- 最终记录明确区分已验证、未验证和人工后续事项，不把硬件 N/A 写成完成。

## 停止条件

- 需要生产凭据、真实 MinIO/Obsidian、真实用户数据、手机或硬件才能继续时立即停止该分支并记为 N/A。
- 发现目标外的大范围架构问题、需要升级依赖或跨模块重构时停止修改，只记录风险。
- 全量测试出现环境性失败时，保留首个完整错误和复现命令；不得通过连接外部服务绕过。
- 任何拟议修改会改变既有状态语义、重试策略或数据兼容性且缺少测试依据时停止并交由调度者裁决。

## 产出路径

- 当前计划：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/plan.md`
- 计划归档：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/产出/plan-2026-08-11.md`
- 执行产出目录：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/产出/2026-08-11-0.5.12离线验收/`
- 执行者回传沿用：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/回传/执行者1.md` 至 `执行者4.md`，仅追加本轮带时间戳记录。
