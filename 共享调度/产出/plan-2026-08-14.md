# 2026-08-14 日清夜跑计划

## 唯一目标

完成“飞书远程控制 Codex”剩余可靠性收口，并在本地证据通过后，以只读任务完成一次受控真实“飞书 → 本地桥接 → Codex → 回传”端到端冒烟验证。

本轮有序范围：

1. 防止 `storage.dataDir` 配置进受管仓库，避免运行文件污染项目工作区。
2. 修复无进度超时可被无效 stdout/stderr 噪声续命的问题，并让 worktree 创建失败的 Git 上下文能回传给移动端/飞书用户。
3. 仅在前两项本地测试通过后，使用受控会话和只读任务做一次真实链路冒烟；任何真实侧条件不满足即停止该分支并标记 N/A。

## 背景与证据

- 已读取当日协商结果：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/协商结果-2026-08-14.md`，状态为“用户已选择唯一目标”。
- 用户在 2026-08-14 16:40 协商中选择“全跑”；协商结果将两项 P2 可靠性问题与一次受控真实只读冒烟定义为同一有序目标。
- 共享任务表 `T-019` 记录：上一轮已修复三项 P1（不完整管理命令误入队、停止竞态、custom provider 认证合同），`49/49` 本地测试、doctor、simulate 与 `git diff --check` 均通过，但整轮仍是 `PARTIAL`，因为真实飞书、真实 Codex 与设备仍为 N/A。
- 剩余已证实风险为：运行目录污染、无进度超时语义、worktree 创建失败上下文回传；本轮只处理这些风险及其直接测试。
- 目标项目为：`/Users/jh-05/Documents/手机远程操控Codex`。当前项目已有未提交改动，涉及配置、runner、bridge、task executor 和相关测试；本轮必须保留既有改动，不覆盖、不回滚、不提交。
- 当前源码与测试入口已显示 `storage.dataDir`、`progressTimeoutMs`、runner stdout/stderr、Git worktree 与移动端报告均有明确实现位置，可支持最小定向修复和回归测试。

## 范围

- 为 `storage.dataDir` 增加与受管项目根目录的冲突校验，阻止运行目录落在任一白名单项目工作区内，并补配置失败测试。
- 将“有效进度”限定为可解析、受认可的结构化 Codex JSONL 事件；无效 stdout/stderr 噪声不得无限重置无进度超时，并补持续噪声与合法进度事件测试。
- 保留 worktree/Git 创建失败的必要、脱敏后的错误上下文，使任务层和移动端/飞书报告能区分 Git 前置失败与 Codex 执行失败，并补失败报告测试。
- 复跑全量 Node 测试、doctor、simulate 与 `git diff --check`，保存实际输出和首个失败（如有）。
- 仅在上述本地门禁全部通过后，在事先确认的受控专用飞书会话中发送一条预先约束的只读任务，验证接收、鉴权、入队、Codex 只读执行与回执闭环。

## 非目标

- 不处理与上述三项无直接依赖的 P2/P3 风险，不借机重构或扩展远程控制能力。
- 不执行远程应用变更、Git 提交、Git 推送、部署、LaunchAgent 安装、数据库迁移、删除工作区或真实业务数据操作。
- 不读取私聊、日历、真实用户数据、生产业务数据或 `config/config.local.json` 的敏感值。
- 不执行实板、烧录、供电/功耗、麦克风、TF、BLE、Reset 或实机 App 联调；历史实板验收 `T-022` 仍为独立阻塞人工任务，不能用本轮软件结果代替。
- 不把真实冒烟计划、本地构建通过或单项测试通过写成整轮 `PASS`。

## 执行者分工

### 执行者1：运行目录安全守卫

- 审计并修改 `src/core/config.ts`、`src/core/security.ts`、`src/core/types.ts`、`config/config.example.json` 与 `test/core-config.test.ts` 的直接相关部分。
- 实现 `storage.dataDir` 与受管项目目录的冲突拒绝，覆盖相同路径、子目录、父目录和正常外置目录。
- 在自己的任务会话回传状态、已读输入、修改文件、测试命令与结果、N/A/阻塞项、下一步和绝对截止时间；不直接写共享调度文件。

### 执行者2：有效进度与无进度超时

- 审计并修改 `src/runner/codex-runner.ts`、`src/runner/jsonl.ts` 和 `test/runner-codex.test.ts` 的直接相关部分。
- 让无效 stdout/stderr 噪声不再续命，只有受认可的结构化进度事件可重置计时器。
- 在自己的任务会话回传状态、实际证据、修改文件、测试命令与结果、N/A/阻塞项、下一步和绝对截止时间；不直接写共享调度文件。

### 执行者3：worktree 失败报告链路

- 审计并修改 `src/workspace/git-worktree.ts`、`src/service/task-executor.ts`、`src/service/bridge-service.ts`、`src/reporting/*` 和对应测试的直接相关部分。
- 保留必要、脱敏的 Git/worktree 失败上下文并验证移动端报告可读，不泄露本地敏感路径或凭据。
- 在自己的任务会话回传状态、实际证据、修改文件、测试命令与结果、N/A/阻塞项、下一步和绝对截止时间；不直接写共享调度文件。

### 执行者4：质量门禁与受控真实冒烟

- 等待前三项回传后，独立复跑全量测试、doctor、simulate、`git diff --check` 与必要的定向失败测试。
- 仅在本地门禁通过后核验受控专用会话、用户身份、白名单、只读任务文本与停止条件；禁止读取私聊或敏感配置值。
- 真实冒烟只允许一条预先约束的只读任务，例如“只读检查当前项目状态并报告风险”；记录时间、发送身份、会话 ID、消息 ID、脱敏结果和 N/A/阻塞原因。
- 在自己的任务会话回传完整终审证据和绝对截止时间；共享计划、任务表、回传目录和产出目录由调度者统一落盘。

## 依赖顺序

1. 执行者1、执行者2、执行者3可并行完成三项本地可靠性收口。
2. 调度者收敛共享接口冲突，只允许与目标直接相关的最小修改。
3. 执行者4在三项本地回传齐全后复跑全量门禁。
4. 只有全量门禁通过且专用会话/身份/只读边界均已核验，才允许执行一次真实冒烟。
5. 真实侧任一条件不满足时停止真实分支；本地结果按实际标为 PARTIAL，不把 N/A 伪装为通过。

## 约束

- 只读取目标项目、共享调度区、当天协商结果和必要的本地测试输出。
- 保留目标项目已有未提交改动，不覆盖、不回滚、不提交、不推送目标项目源码。
- 所有真实链路操作必须使用用户身份、预先约束的受控专用会话和只读任务；不得改用机器人身份，不得发送到私聊或不明会话。
- 不输出、记录或传播 `config/config.local.json` 中的 secret/token；报告只保留字段是否存在、是否通过与脱敏错误。
- 外部服务、真实飞书、真实 Codex、网络或权限异常均按真实结果记录；禁止靠关闭检查或伪造回执绕过。

## 测试

- 定向配置测试：`node --test --experimental-strip-types test/core-config.test.ts`。
- 定向 runner 测试：`node --test --experimental-strip-types test/runner-codex.test.ts`。
- 定向任务/报告测试：`node --test --experimental-strip-types test/service-e2e.test.ts test/workspace.test.ts`。
- 全量测试：`node --test --experimental-strip-types test/*.test.ts`。
- Doctor：`node --experimental-strip-types scripts/doctor.ts`。
- 本地模拟：`node --experimental-strip-types src/cli/simulate.ts '项目 demo：只读检查当前项目状态并报告风险'`。
- Git 检查：`git diff --check`、`git status --short` 与必要的 `git diff --stat`。
- 真实冒烟（仅在前述门禁通过后）：在受控专用会话发送一条只读任务，按消息接收、鉴权、入队、运行、回执五个节点保存脱敏证据。

## 验收标准

- `storage.dataDir` 无法配置在任一受管仓库内或与其父子目录重叠，外置安全路径可正常通过。
- 无效 stdout/stderr 持续输出不能阻止无进度超时；合法结构化进度事件仍可续命。
- worktree/Git 创建失败会产生可读、脱敏且可定位的移动端/飞书错误上下文。
- 定向与全量测试、doctor、simulate、`git diff --check` 的实际结果均被记录；任一失败不伪称 PASS。
- 真实冒烟若执行，必须证明只读任务从受控会话到回执的实际链路；若未执行，必须明确 N/A/阻塞原因。
- 全轮最终状态只能由证据判定为 PASS、PARTIAL 或 FAIL；未完成真实链路时最高为 PARTIAL。

## 停止条件

- 本地任一门禁失败，停止真实冒烟分支，保留首个失败与复现命令。
- 无法确认目标会话为受控专用会话、发送身份不为当前用户、白名单不匹配、任务不再是只读，或需要读取敏感配置值时，停止真实冒烟。
- 真实飞书、真实 Codex、网络或权限异常时，不重试到超出约束，不改用机器人身份，记录 N/A/阻塞。
- 任何需求扩大到生产服务、部署、提交/推送、删除、实板操作或真实用户数据时，立即停止并等待用户另行授权。

## 产出路径

- 当前计划：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/plan.md`
- 计划归档：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/产出/plan-2026-08-14.md`
- 执行产出目录：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/产出/2026-08-14-飞书远程控制Codex-P2与受控冒烟/`
- 内容来源：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/协商结果-2026-08-14.md`
- 实板模板参考（本轮不适用）：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/产出/实板验收任务模板.md`
