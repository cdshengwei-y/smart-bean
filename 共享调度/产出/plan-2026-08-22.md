# 2026-08-22 日清夜跑计划

> 本文件为当天 `plan.md` 的归档副本。当天协商结果文件缺失，目标沿用上一轮已确认目标。

## 唯一目标

收口远程 Codex 可靠性与可用性交互：完善连续 180 秒无有效进度的诊断与 `changedFiles` 记录；提供失败任务详情和仅限安全幂等任务的受控重试；补齐飞书轮询/TLS、Codex 可执行性和队列卡死的自检、告警与状态保留恢复。

## 背景、范围、非目标

- 协商结果 `/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/协商结果-2026-08-22.md` 缺失，按上一轮证据继续；上一轮真实只读任务在 `180000ms` 无有效进度后失败且缺少显式 `changedFiles=[]`。
- 范围：超时分阶段诊断与变更证据、失败详情与安全幂等重试、轮询/TLS/Codex/队列自检告警和受控恢复、本地模拟与回归。
- 非目标：真实外部测试、批量重试、生产部署/配置、数据库迁移、硬件操作、真实用户数据和业务项目 Git 操作。

## 执行者1-4、依赖与约束

1. 执行者1固化诊断和 `changedFiles` 合同；执行者2实现失败详情与重试判定；执行者3实现自检告警和状态保留恢复；执行者4独立门禁与状态机审计。
2. 依赖顺序：诊断合同 -> 失败反馈/重试 -> 自检恢复 -> 本地模拟回归 -> 独立终审。
3. 仅记录脱敏上下文；重试必须安全、幂等、可验证；执行者只回传，调度者统一落盘；真实链路与本地结果分开。

## 测试、验收与停止条件

- 测试：定向 Node 测试、`node --test --experimental-strip-types test/*.test.ts`、doctor、simulate、`git diff --check`、`git status --short`。
- 验收：失败任务可回读阶段/原因/`changedFiles`/下一步；安全重试有效且危险或重复任务拒绝；自检覆盖轮询/TLS、Codex、队列异常；真实链路无授权则 `N/A`。
- 停止：扩大到真实飞书/生产/硬件/业务 Git 操作，无法保留状态或出现未知副作用，任一本地门禁失败或敏感数据风险。

## 产出路径

- 当前计划：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/plan.md`
- 归档：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/产出/plan-2026-08-22.md`
- 目标项目：`/Users/jh-05/Documents/手机远程操控Codex`
