# 2026-08-21 日清夜跑计划

## 唯一目标

远程 Codex 可靠性与可用性交互收口：完善连续 180 秒无有效进度的诊断与任务记录，为失败任务提供受安全边界约束的可操作反馈和重试入口，并补齐常驻服务对飞书轮询/TLS、Codex 可执行性与队列卡死的自检、告警和受控恢复。

## 背景

- 当天协商结果已确认唯一目标，用户明确选择“全跑”。
- 2026-08-20 真实受控只读任务的回执链正确，但 Codex 连续 `180000ms` 无有效进度后失败，且任务记录未显式保存 `changedFiles=[]`；上一轮状态为 `FAILED`。
- 上一轮本地门禁 `82/82`、doctor、simulate、`git diff --check` 通过，不代表真实外部执行恢复。

## 范围

- 区分启动、TLS/网络、认证、模型服务、子进程和队列的无进度故障，保存脱敏诊断与 `changedFiles` 证据。
- 提供失败任务详情和仅限安全、幂等、可验证任务的受控重试；拒绝副作用、已取消、变更不明或缺证据的重试。
- 增加飞书轮询/TLS、Codex、队列卡死的服务自检、告警与状态保留的受控恢复。

## 非目标

- 不执行真实飞书/Codex 测试、批量重试、生产操作、数据库迁移、硬件操作或业务项目 Git 操作。
- 不把本地测试通过写成真实链路成功，不用重启掩盖任务丢失。

## 执行者1-4分工

1. 执行者1：固化超时诊断、脱敏报告与 `changedFiles` 合同。
2. 执行者2：实现失败详情和安全重试判定。
3. 执行者3：实现服务自检、告警与保留幂等/状态的受控恢复。
4. 执行者4：独立复跑门禁并审计状态机和副作用边界。

## 依赖、约束与测试

- 先诊断合同，再失败反馈/重试，再自检恢复，最后独立终审。
- 重试和恢复必须保留任务 ID、幂等键、状态、失败阶段与变更证据；敏感信息只保留脱敏上下文。
- 覆盖启动失败、无进度、合法进度、网络/TLS、队列卡死、恢复、查询、允许/拒绝重试、取消和重复指令。
- 运行定向测试、`node --test --experimental-strip-types test/*.test.ts`、doctor、simulate、`git diff --check`；业务项目不提交。

## 验收、停止与产出

- 失败任务能回读失败阶段、脱敏原因、`changedFiles` 与下一步；安全重试可执行，危险/不确定任务被拒绝；模拟异常可被自检发现且恢复不丢状态。
- 未经单独授权不做真实链路测试；真实外部链路保持 `N/A`。
- 任一门禁失败、状态/幂等无法保留、存在敏感泄露或需求扩大即停止。
- 产出为当前计划、今日归档、执行者回传和本地验收材料。
