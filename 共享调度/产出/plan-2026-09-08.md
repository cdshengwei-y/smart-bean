# 2026-09-08 日清夜跑计划

> 本文件为当天 `共享调度/plan.md` 的归档副本，采用 16:56 读取的用户协商结果。

## 唯一目标与范围

完成声呐 V0.2 软件测距与地形数据最小闭环：用模拟 `VOUT` 波形实现 ToF 回波检测、距离计算、`timestamp_ms,depth_m,temperature_c,status` CSV 输出和基础二维/三维绘图；覆盖正常回波、无回波、噪声、多回波。

## 非目标与限制

不接线、不烧录、不做水下测试，不承诺真实硬件或 10 m/30 m/100 m 量程；不做复杂地图、生产部署、业务项目 Git 推送或线上修改。模拟结果不能替代真实换能器、TUSS4470、STM32 ADC、盲区和多径验收。

## 分工、依赖与验收

执行者1定义波形参数，执行者2完成 ToF/距离检测，执行者3完成 CSV/绘图和证据，执行者4独立复核并裁定 PASS/PARTIAL。顺序为参数与波形、检测换算、数据与图、四类测试和复核。验收要求闭环可重复、字段完整、图可读、测试证据可追溯；未执行真实硬件验收标 N/A。

## 约束、停止条件与产出

仅使用已授权资料和本地模拟数据，不读取私聊、生产服务或真实用户数据，不修改业务项目。若需真实硬件/未授权资源、参数或结果不可追溯、字段缺失或证据无法保存则停止。测试证据目录为 `产出/2026-09-08-sonar-v02/`。
