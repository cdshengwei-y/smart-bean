# 2026-08-26 日清夜跑计划

> 本文件为当天 `plan.md` 的归档副本，目标采用 16:40 协商结果中用户确认的唯一目标。

## 唯一目标

收口远程 Codex 在连续 `180000ms` 无有效进度时的失败诊断：显式保存失败阶段、脱敏失败原因和 `changedFiles`，覆盖启动、网络/TLS、认证、模型服务、子进程和队列阶段。

## 背景、范围、非目标

- 2026-08-20 真实受控只读任务超时失败，隔离 worktree 无变更但缺少显式 `changedFiles=[]`。
- 范围：失败诊断与变更证据合同、终态落盘/回读、本地多阶段 fixture 和回归测试。
- 非目标：真实生产任务、真实飞书/Codex 链路、批量/无限重试、硬件、生产服务和业务项目 Git 操作。

## 执行者、依赖与约束

1. 执行者1固化诊断合同；执行者2实现终态落盘和查询；执行者3补齐六类失败 fixture 与回归；执行者4独立终审。
2. 依赖顺序：读取输入 -> 诊断合同 -> 终态落盘 -> 本地测试 -> 独立审计。
3. 诊断脱敏；`changedFiles` 显式记录；执行者只回传，调度者统一写共享文件；本地结果与真实链路分开。

## 测试、验收与停止条件

- 测试：定向状态机/存储/报告测试、全量 `node --test`（若存在）、doctor、simulate、`git diff --check`。
- 验收：失败阶段/原因/类型/任务 ID/`changedFiles` 可回读；无变更为 `[]`；六类 fixture 有证据；外部链路无证据则 N/A。
- 停止：扩大到真实飞书/生产/硬件/业务 Git，状态或幂等证据无法保存，任一本地门禁失败或出现敏感信息风险。

## 产出路径

- 当前计划：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/plan.md`
- 归档：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/产出/plan-2026-08-26.md`
- 目标项目：`/Users/jh-05/Documents/手机远程操控Codex`
