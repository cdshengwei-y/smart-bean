# 2026-09-09 日清夜跑计划

> 本文件为当天 `共享调度/plan.md` 的归档副本，采用 16:40 读取的用户协商结果。

## 目标

完成声呐 V0.2 三项收口：修正 30 m/5 mm 模拟标定误差；沉淀报告、参数、结果、图表和限制到声呐知识库；固化 STM32 串口/CSV 合同并用本地 fixture 验证采样输入—测距—绘图接口。

## 已知基线

昨日四类模拟场景、CSV、JSON、波形和二维/三维图已可复核；30 m 临时结果为 `29.985072 m`，误差 `14.928 mm`，超过 `5 mm`，因此昨日整体为 `PARTIAL`。

## 执行与验收

先复现基线并修正采样/时间标定，保留前后证据，再跑四类回归；随后整理声呐知识库和来源/去重记录；最后冻结版本化串口/CSV 合同，覆盖字段、单位、状态、空值、坏数据和 fixture 端到端绘图。30 m 误差须不超过 `0.005 m`，否则保留失败证据并标记 `PARTIAL`。

## 限制与产出

仅做本地模拟、文档和 fixture，不接线、供电、烧录或水下测试，不修改业务项目。真实硬件与人工实板验收为 `N/A`。产出目录：`共享调度/产出/2026-09-09-sonar-v02-closeout/`；昨日基线：`共享调度/产出/2026-09-08-sonar-v02/`。
