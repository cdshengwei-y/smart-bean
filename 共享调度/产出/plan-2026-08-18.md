# 2026-08-18 日清夜跑计划

## 唯一目标

优化“飞书远程控制 Codex”的回复速度，缩短从群消息被本地桥接接收，到首条有效回复/回执生成的耗时。

本轮只做本地可控阶段的时延基线、最小优化和回归验证；不承诺或伪造飞书网络、外部 Codex 推理或模型排队等不可控耗时的改善。

## 背景与证据

- 2026-08-18 16:40 协商结果已明确选择“优化回复速度，缩短从群消息被本地桥接接收，到首条有效回复/回执生成的耗时”。
- 白天已核验 2026-08-18 机器人受控群消息闭环：机器人发送一次、回读正文/身份/群 ID/时间均通过，任务表 T-035～T-038 最终 PASS；这些是白天已完成事实，不等同于本轮性能优化已完成。
- 目标项目 `/Users/jh-05/Documents/手机远程操控Codex` 当前存在未提交修改，已有消息适配、鉴权、任务入队、Codex 启动、报告解析、回执发送和本地模拟测试入口；本轮必须保留现有修改，不提交或推送业务项目。
- 现有实现可观察的阶段包括 `RemoteBridge.handleMessage`、任务入队/领取、`runCodex` 启动与 JSONL 首个有效事件、结构化报告读取、最终回执发送；当前尚未形成统一的阶段耗时基线。

## 范围

- 审计并补充本地可控阶段的单调时钟埋点或测试计时：消息接收、鉴权/解析、入队、领取、执行启动、首个有效进度/回执准备、报告解析和回执发送。
- 用本地模拟或受控测试建立同一场景的优化前基线，记录实际数字、测试环境、样本数和首个失败。
- 只针对可控瓶颈实施最小优化，例如减少不必要的串行等待、提前准备首条有效回执所需数据或复用已存在的阶段结果；不得绕过鉴权、幂等、终态证据或失败报告。
- 补充回归测试，覆盖成功、失败、取消、重复消息、通知重试和无进度超时等相邻边界。
- 复跑与修改相关的定向测试、全量 Node 测试、doctor、simulate 和 `git diff --check`，如实判定 PASS、PARTIAL 或 FAIL。

## 非目标

- 不承诺飞书网络、外部 Codex 推理、模型排队、第三方 Provider 或真实手机链路的时延改善。
- 不使用真实用户任务做压力测试，不读取私聊、日历、真实用户数据或生产服务。
- 不重复发送问候语或其他真实群消息；不以机器人或用户身份进行额外外部写操作。
- 不改动业务方向，不做部署、烧录、硬件操作、数据库写入、Git 提交或 Git 推送。
- 不把一次构建成功、单项测试通过或历史机器人消息 PASS 写成整轮性能优化完成。

## 执行者分工

### 执行者 1：阶段时延审计与基线

- 审计 `src/adapters/feishu*.ts`、`src/service/bridge-service.ts`、`src/core/task-store.ts`、`src/runner/codex-runner.ts` 和现有模拟入口。
- 设计统一的阶段名称、单调时钟口径和最小脱敏证据格式。
- 在不改变业务语义的前提下建立可重复基线；回传实际数字、样本数、环境、命令、N/A/阻塞项和绝对截止时间，不直接写共享调度文件。

### 执行者 2：最小可控优化

- 依据执行者 1 的基线，只选择一个证据最充分的本地瓶颈实施最小改动。
- 保留身份鉴权、幂等去重、任务状态机、工作区隔离、终态证据和失败脱敏。
- 回传修改文件、优化前后同场景数据、测试命令与结果、N/A/阻塞项和绝对截止时间，不直接写共享调度文件。

### 执行者 3：回归测试与边界覆盖

- 补充或调整消息接收/入队、Codex 启动与进度、首条回执准备和通知重试相关测试。
- 至少覆盖成功、失败、取消、重复消息、无进度超时和已有最终通知重试；确认测试不会依赖真实飞书或真实 Codex。
- 回传逐项结果、实际证据、未覆盖边界、下一步和绝对截止时间，不直接写共享调度文件。

### 执行者 4：独立质量门禁

- 等待前三项回传后，独立复跑定向测试、全量 Node 测试、doctor、simulate 和 `git diff --check`。
- 审计阶段计时是否使用单调时钟、是否把首条有效回复与最终完成混淆、是否跳过安全门禁，以及既有未提交改动是否被保留。
- 给出最终 PASS/PARTIAL/FAIL、证据索引、N/A/阻塞项、下一步和绝对截止时间；不直接写共享调度文件。

## 依赖顺序

1. 执行者 1 先固定阶段定义和优化前基线。
2. 执行者 2 依据基线实施一个最小可控优化；执行者 3 在接口明确后补回归测试。
3. 执行者 4 在前三项证据齐全后独立复跑全部本地门禁。
4. 调度者统一汇总回传、更新任务表、落盘计划与归档；任何证据不足时保留 PARTIAL/阻塞。

## 约束

- 所有时延数字必须注明时间口径、样本数、运行环境和命令；不能用主观“更快”替代数字。
- 只使用已授权的目标项目、共享调度区、当日协商结果、历史回传和本地模拟/测试输出。
- 不输出 token、secret、Cookie、私钥、真实配置值或未经脱敏的本地敏感内容。
- 保留目标项目现有未提交改动；执行者不直接写 `plan.md`、任务表、回传目录或产出目录。
- 真实飞书、真实 Codex、生产服务、外部网络或硬件边界若无法核验，必须标记 N/A，不得用本地模拟冒充真实链路。

## 测试与核验

- 定向阶段/服务测试：按实际修改路径运行相关 `test/*.test.ts`，记录逐项结果。
- 全量测试：`node --test --experimental-strip-types test/*.test.ts`。
- Doctor：`node --experimental-strip-types scripts/doctor.ts`。
- 本地模拟：使用 `src/cli/simulate.ts` 或现有受控 fixture，记录接收至首条有效回执准备的阶段时延；不连接真实服务。
- Git 检查：`git diff --check`、`git status --short` 和必要的 `git diff --stat`；只检查业务项目，不提交或推送。
- 提交前字段自检：今日总结含白天真实事实；输入材料含 Git 链接、归档说明和自查状态；协调栏含网络/权限阻塞及下一步；`人员状态`填“早班”。

## 验收标准

- 形成可重复的优化前/优化后阶段时延证据，至少能区分接收、鉴权/解析、入队、执行启动、首条有效回复准备和最终回执发送。
- 至少一个本地可控阶段有明确、可复核的改善，或有证据说明没有安全可接受的改善空间并将状态记为 PARTIAL。
- 身份鉴权、幂等、任务终态、取消、失败脱敏和通知重试语义无回归。
- 定向/全量门禁及 doctor、simulate、`git diff --check` 的实际结果全部如实记录；真实飞书/外部 Codex 时延仍单列为 N/A。
- 目标项目未提交改动被保留，未发生业务 Git 提交、推送、部署或生产写操作。

## 停止条件

- 无法建立可重复的本地基线或无法区分首条有效回复与最终完成时，停止优化并标记 PARTIAL。
- 任一本地门禁失败，停止继续扩展，只保留首个失败和复现命令。
- 优化需要跳过鉴权、幂等、状态机、失败报告或安全确认时立即停止。
- 需求扩大到真实用户数据、生产服务、真实外部链路、部署、Git 提交/推送或硬件操作时立即停止并等待另行授权。

## 产出路径

- 当前计划：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/plan.md`
- 计划归档：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/产出/plan-2026-08-18.md`
- 当天协商结果：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/协商结果-2026-08-18.md`
- 目标项目：`/Users/jh-05/Documents/手机远程操控Codex`
- 目标表格：`https://acn9omuig41v.feishu.cn/base/JB1abJgEDafVMYsJRpvcj2Gdnch?table=tblSCx7LZImh0nF9`
