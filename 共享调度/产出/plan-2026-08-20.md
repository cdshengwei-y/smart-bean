# 2026-08-20 日清夜跑计划

## 唯一目标

重新安排并验证一次真实受控只读任务闭环：使用不会触发夜跑状态查询的显式只读命令，确认任务入队后先返回“已接收，正在处理”，再按顺序产生阶段/最终回执，并核验机器人身份、目标群、回执顺序和 `changedFiles=[]`。若权限、常驻服务或真实链路条件不满足，严格记为 `PARTIAL/N/A`，不伪造 `PASS`。

## 背景与证据

- 今日协商结果文件缺失，故沿用上一轮明确遗留的唯一目标，不宣称已完成协商。
- 2026-08-19 真实测试消息被旧解析规则误判为夜跑状态查询，未创建任务；真实任务回执链和 `changedFiles=[]` 尚无证据。上一轮最终状态为 `PARTIAL`。
- 目标项目今日本地门禁已核验：全量测试 `82/82` 通过，`doctor` 退出码 0；这些本地结果不等同于真实飞书链路通过。

## 范围

- 复核正确“叶聪宇”受控群、用户与机器人身份、成员关系、服务运行态和重复发送风险。
- 选择不含“夜跑/报告”状态查询关键词的显式只读命令，且预期无文件变更。
- 回读受理、阶段和最终回执，记录消息 ID、身份、群 ID、顺序与 `changedFiles`。
- 复跑本地测试、doctor、simulate 和 `git diff --check`，如实汇总 `PASS/PARTIAL/FAIL`。

## 非目标

- 不压力测试，不发送重复消息，不读取私聊、日历、真实用户数据或生产服务。
- 不提交/推送业务项目，不部署、不烧录、不连接真实硬件或生产服务。

## 执行者1-4分工

1. 执行者1核验群、身份、服务与只读边界。
2. 执行者2核验受理回执正文、落库/唤醒和失败语义。
3. 执行者3执行一次真实只读命令并回读全链路证据；条件不满足记 `N/A/阻塞`。
4. 执行者4独立复跑门禁并审计最终状态，不直接写共享文件。

## 依赖顺序、约束、测试、验收与停止条件

- 先核验身份/权限/服务，再执行一次真实命令，最后独立终审；任一硬门禁失败立即停止。
- 受理正文必须是“已接收，正在处理”；真实测试只允许目标群一次；身份、群 ID、顺序或 `changedFiles` 不符即停止。
- 本地测试：`node --test --experimental-strip-types test/*.test.ts`；`node --experimental-strip-types scripts/doctor.ts`；`node --experimental-strip-types src/cli/simulate.ts`；`git diff --check`。
- 验收要求真实任务创建、受理先于阶段/最终回执、正确身份/群 ID、`changedFiles=[]`；缺证据整轮最高为 `PARTIAL`。
- 产出：当前计划、今日归档、回传目录和材料仓库；目标项目现有未提交修改全部保留。
