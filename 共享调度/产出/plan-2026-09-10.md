# 2026-09-10 日清夜跑计划

> 本文件为当天 `共享调度/plan.md` 的归档副本，采用用户已确认的唯一目标“今天全跑”。

## 目标

完成声呐下一阶段三项本地工作：增加温度/声速变化、噪声和多径鲁棒性回归；实现 STM32/UART 实时数据适配器和连续采样演示；完成真实硬件接入前的电源、引脚、换能器和安全验收包。

## 已知基线

2026-09-09 本地 V0.2 范围 PASS：30 m 已由误差 `-14.928 mm` 修正到 `+0.266 mm`，五类模拟场景、`sonar-v02-csv-1` fixture 和基础图通过。真实 UART、ADC、供电、换能器、烧录和水下测试均仍为 N/A。

## 执行与验收

先将温度/声速、噪声和多径的全部假设参数化并保留可重复 fixture；在不改变 `sonar-v02-csv-1` 字段、单位、版本和状态枚举的前提下，实现连续本地 UART 行解析、标准 CSV 输出、坏行诊断和基础绘图；随后整理硬件接入前检查包，逐项列出来源、待确认项、上电/停机边界及证据位置。30 m 误差必须不超过 `0.005 m`，否则保留失败证据并判 PARTIAL。

## 实板边界

本轮只做模拟、协议与资料包，不接线、供电、烧录或读取真实 UART/ADC。实板人工负责人和北京时间绝对截止均未指定，故实板验收任务为 `BLOCKED / N/A`，不能以清单或本地演示替代实测。

## 产出

证据目录：`共享调度/产出/2026-09-10-sonar-next-stage/`。产出包括版本化 fixture、参数和回归结果、UART 适配器与诊断、CSV/图表、硬件接入前验收包及验收摘要；不修改业务项目源码或生产环境。
