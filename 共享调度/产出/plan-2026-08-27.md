# 2026-08-27 日清夜跑计划

> 本文件为当天 `plan.md` 的归档副本，目标采用 16:40 协商结果中用户确认的“1，2，3全做”。

## 唯一目标

收口远程 Codex 失败诊断的安全与证据语义：修复恢复脱敏、`changedFiles` 采集状态和 `180000ms` 无有效进度的保守归因，并完成独立终审。

## 背景、范围、非目标

- 上一轮独立终审发现恢复/通知重试脱敏、worktree 采集状态和超时阶段归因三项风险。
- 范围：三项修复、恢复/重试与多阶段超时 fixture、定向/全量门禁和独立终审。
- 非目标：真实飞书/Codex、生产、外部扩容、批量重试、硬件、用户数据和业务项目 Git 操作。

## 执行者、依赖与约束

1. 执行者1修复脱敏；执行者2修复 `changedFiles` 状态；执行者3修复保守归因与 fixture；执行者4独立终审。
2. 依赖顺序：输入读取 -> 合同修复 -> fixture/回归 -> 独立终审。
3. 敏感原因不得持久化或重发；`[]` 与 `N/A/false` 必须区分；保留既有未提交改动；执行者只回传。

## 测试、验收与停止条件

- 测试：定向脱敏/存储/报告/恢复/重试测试、全量 `node --test`（若存在）、doctor、simulate、`git diff --check`。
- 验收：敏感 fixture 不可回读；三种 `changedFiles` 状态准确；六类阶段和未知无进度均有证据；外部链路无证据则 N/A。
- 停止：P1 未消除、门禁失败、敏感泄露风险、状态无法保存，或扩大到真实外部/生产/硬件/业务 Git。

## 产出路径

- 当前计划：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/plan.md`
- 归档：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/产出/plan-2026-08-27.md`
- 目标项目：`/Users/jh-05/Documents/手机远程操控Codex`
