# 2026-08-13 日清夜跑计划

## 唯一目标

今晚夜跑“全跑”飞书远程控制 Codex 的三项 P1 风险，并在完成后按协商结果向飞书“叶聪宇”群发送一句：“我准备好了。”

三项 P1 风险：

1. 拦住不完整管理命令误入队。
2. 修掉停止信号与领取任务的竞态。
3. 补上 custom provider 认证闭环。

## 背景与证据

- 已读取 2026-08-13 协商结果文件：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/协商结果-2026-08-13.md`，状态为“用户已选择唯一目标”。
- 用户在 16:40 协商任务中要求：“全跑，然后目标设置为完成后在飞书给我发一句‘我准备好了’。”
- 共享任务表最新一轮 `T-019 飞书远程控制 Codex 本地桥接优化验收与收口` 已完成但状态为 `PARTIAL`，终审确认仍有三项 P1 风险。
- 2026-08-12 验收摘要明确记录：命令解析、飞书适配器、配置合同、Codex runner、任务执行与模拟回执已通过现有本地测试，但由于三项 P1 与三项 P2 风险存在，不能升为 `PASS`。
- 目标项目为：`/Users/jh-05/Documents/手机远程操控Codex`。
- 当前目标项目已有未提交改动，涉及 `CHANGELOG.md`、`config/config.example.json`、`src/adapters/feishu-command.ts`、`src/adapters/feishu.ts`、`src/cli/simulate.ts`、`src/core/config.ts`、`src/core/index.ts`、`src/core/types.ts`、`src/main.ts`、`src/runner/codex-runner.ts`、`src/service/bridge-service.ts`、`src/service/task-executor.ts` 和相关测试。今晚执行必须保留这些既有改动，不覆盖、不回滚、不提交。
- 项目 README 明确安全边界：当前 MVP 支持接收、鉴权、排队、取消、隔离执行和报告；不支持远程应用变更、提交、推送、部署或删除工作区。

## 范围

- 修复命令解析：在单项目自然语言兜底前识别并拒绝不完整中文管理命令，避免“确认应用”“放弃”等缺少任务 ID 的管理意图误入任务队列。
- 修复停止竞态：确保服务停止信号在领取任务前后均生效，停止期间不再领取或执行新任务，已领取但未登记 controller 的窗口需要可取消或安全退出。
- 补齐 custom provider 认证闭环：明确 custom provider 所需认证变量或 `env_key` 配置合同，保证配置、runner、示例配置和失败提示一致，并避免把敏感值写入报告。
- 补充对应测试：优先补命令解析、任务执行停止、runner custom provider 配置与错误路径测试。
- 复跑本地全量测试、doctor、simulate 和 Git 污染检查，形成可复现证据。
- 夜跑完成且证据满足后，按协商结果在飞书“叶聪宇”群发送“我准备好了”；若群名、权限、真实飞书发送或生产边界不满足，则停止该通知分支并报告原因。

## 非目标

- 不处理 P2 风险，除非 P1 修复必须触及且能以最小安全方式顺手补齐。
- 不连接生产服务，不读取私聊、日历、真实用户数据或生产业务数据。
- 不提交、不推送目标项目源码 Git，不创建 PR，不部署，不安装或启用 LaunchAgent。
- 不启用远程应用变更、提交、推送、部署或删除工作区能力。
- 不把计划写成已完成；真实完成情况由次日复盘读取夜跑产出后再写。

## 执行者分工

### 执行者1：命令入口与管理命令拦截

- 审计并修改 `src/adapters/feishu-command.ts`、`src/service/bridge-service.ts` 和相关测试。
- 明确不完整中文管理命令的拒绝策略和用户提示。
- 输出命令解析矩阵、拒绝样例和定向测试结果。

### 执行者2：停止信号、领取任务与状态机

- 审计并修改 `src/service/task-executor.ts`、`src/service/bridge-service.ts`、`src/core/state-machine.ts` 和端到端测试。
- 验证 shutdown/stop 与 claim/controller 登记之间没有继续执行新任务的窗口。
- 输出竞态复现、修复点和测试证据。

### 执行者3：custom provider 认证闭环

- 审计并修改 `src/core/config.ts`、`src/core/types.ts`、`src/runner/codex-runner.ts`、`config/config.example.json` 和 runner/config 测试。
- 明确 custom provider 的认证变量配置、传递方式、缺失错误和脱敏边界。
- 输出 runner 参数清单、失败路径和定向测试结果。

### 执行者4：独立复核与交付记录

- 复核前三项 P1 修复是否均有代码证据和测试证据。
- 复跑 `node --test --experimental-strip-types test/*.test.ts`、`node --experimental-strip-types scripts/doctor.ts`、simulate 和 `git diff --check`。
- 生成最终验收记录，明确 PASS/PARTIAL/FAIL、未验证项、风险和是否已发送“我准备好了”。

## 依赖顺序

1. 执行者1、执行者2、执行者3可并行审计各自 P1 风险。
2. 若三项修复有共享配置或状态机接口冲突，由调度者收敛到最小一致合同。
3. 执行者4等待三项修复和测试结果后终审。
4. 只有终审证据满足后，才执行飞书“我准备好了”通知分支。
5. 调度者最后汇总产出，不提前把真实飞书闭环、真实通知或 PASS 写成已完成。

## 约束

- 只读取 `/Users/jh-05/Documents/手机远程操控Codex`、共享调度区、当天协商结果和必要的本地测试输出。
- 保留目标项目已有未提交改动，不覆盖、不回滚、不提交。
- 不输出或传播 `config/config.local.json` 中的敏感配置值；如需引用，只描述字段存在、缺失或用途。
- 测试优先使用本地模拟或假进程；不得为了通过测试连接生产服务。
- 所有通过数、失败原因和行为结论必须来自实际命令输出；不确定项写 N/A 或待人工确认。

## 测试

- 全量测试：在 `/Users/jh-05/Documents/手机远程操控Codex` 执行 `node --test --experimental-strip-types test/*.test.ts`。
- Doctor：执行 `node --experimental-strip-types scripts/doctor.ts`。
- 本地模拟：执行 `node --experimental-strip-types src/cli/simulate.ts '项目 demo：只读检查当前项目状态并报告风险'` 或等价只读任务。
- Git 检查：执行 `git diff --check`、`git status --short` 和必要的 `git diff --stat`。
- 定向测试：复跑或新增 `test/feishu-command.test.ts`、`test/service-e2e.test.ts`、`test/runner-codex.test.ts`、`test/core-config.test.ts`。

## 验收标准

- 三项 P1 均有明确修复证据、测试证据和移动端可理解的错误/状态反馈。
- 不完整中文管理命令不会被单项目兜底误建为任务。
- 停止期间不会领取并执行新任务，竞态测试可复现旧问题并验证新行为。
- custom provider 认证配置从示例、配置解析、runner 参数到缺失错误形成闭环，且不泄露敏感值。
- 全量 Node 测试实际通过；若失败，记录首个失败、复现命令和归因。
- 不发生目标项目 Git 提交/推送、部署、LaunchAgent 安装或生产服务连接。
- 若飞书“我准备好了”通知已发送，必须记录目标会话、发送身份、消息 ID 和发送时间；若未发送，必须记录停止原因。

## 停止条件

- 需要真实生产飞书群、真实 Codex 远程执行、生产凭据、手机实机、外部部署或用户二次确认才能继续时，立即停止对应分支并标记 N/A。
- 发现未提交改动来源不明且会被夜跑覆盖时，停止修改，只做只读审计并报告冲突。
- 任一 P1 修复会扩大到远程应用、提交、推送、部署或删除工作区能力时，停止该分支。
- Git 可访问 PLAN.MD 链接无法生成或自查失败时，停止飞书写入并报告原因，不用本地路径或飞书文件链接冒充。

## 产出路径

- 当前计划：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/plan.md`
- 计划归档：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/产出/plan-2026-08-13.md`
- 执行产出目录：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/产出/2026-08-13-飞书远程控制Codex-P1收口/`
- 内容来源任务：Codex 任务“日清夜跑16点40”
- 执行任务：Codex 任务“日清夜跑17点”
