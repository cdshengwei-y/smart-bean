# 2026-08-21 日清夜跑计划

## 唯一目标

远程 Codex 可靠性与可用性交互收口：完善连续 180 秒无有效进度的诊断与任务记录，为失败任务提供受安全边界约束的可操作反馈和重试入口，并补齐常驻服务对飞书轮询/TLS、Codex 可执行性与队列卡死的自检、告警和受控恢复。

## 背景

- 当天协商结果 `/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/协商结果-2026-08-21.md` 已确认上述唯一目标，用户明确选择“全跑”。
- 2026-08-20 真实受控只读任务已创建，受理、3 条阶段和失败终态回执的身份、群 ID 与顺序均正确；但 Codex 连续 `180000ms` 无有效进度后失败，任务记录未显式保存 `changedFiles=[]`，上一轮最终状态为 `FAILED`。
- 上一轮本地门禁为 `82/82`、`doctor`、`simulate`、`git diff --check` 通过。该证据仅说明既有本地行为，不代表真实外部执行已恢复。
- 目标项目 `/Users/jh-05/Documents/手机远程操控Codex` 有用户现存未提交修改，必须保留；不提交或推送业务项目。

## 范围

- 将无有效进度超时区分为启动、TLS/网络、认证、模型服务、子进程和队列等可识别阶段，保存脱敏诊断与显式 `changedFiles` 证据。
- 为失败任务提供任务详情、失败原因、下一步和仅限安全、幂等、可验证类型的受控重试入口；拒绝有副作用、已取消、变更不明或缺少证据的重试。
- 增加常驻服务自检：飞书轮询/TLS、Codex 可执行性、运行队列进度和卡死识别；恢复仅限保留幂等键、状态与失败上下文的受控动作。
- 用本地模拟覆盖启动失败、无有效进度、合法进度持续、网络/TLS 异常、队列卡死、恢复、失败任务查询、允许/拒绝重试、取消和重复指令。

## 非目标

- 不执行新的真实飞书/Codex 测试、批量重试、生产部署、数据库迁移、线上配置修改、硬件操作或真实用户数据操作。
- 不用重启掩盖任务丢失，不把本地测试通过表述为真实外部链路成功。
- 不提交或推送业务项目，不部署、不烧录、不连接生产服务。

## 执行者 1-4 分工

1. 执行者1：梳理无进度超时路径和任务记录，设计可区分、脱敏、可回读的诊断与 `changedFiles` 合同。
2. 执行者2：实现失败任务详情与受控重试判定，覆盖允许、拒绝、取消、重复和副作用边界。
3. 执行者3：实现服务自检、告警与受控恢复，验证轮询/TLS、Codex、队列卡死和状态保留。
4. 执行者4：独立复跑定向/全量门禁，审计诊断、重试、恢复、幂等和状态机；不直接写共享调度文件。

## 依赖顺序

1. 先固化诊断与任务证据合同。
2. 基于该合同实现失败反馈和重试判定。
3. 在任务状态/幂等语义明确后实现自检与受控恢复。
4. 完成本地模拟与回归后，由执行者4独立终审；调度者统一落盘共享文件。

## 约束

- 诊断只记录必要的脱敏上下文，不能泄露凭据、真实用户数据或完整环境配置。
- 自动/手动重试仅允许明确安全、幂等且可验证的任务类型；其余任务保持失败并说明原因。
- 恢复前后必须保留任务 ID、幂等键、状态、失败阶段和变更证据；不得重复发送外部副作用。
- 所有本地验证与真实飞书/Codex 结果分别记录；执行者仅回传，不直接写共享计划或产出。

## 测试

- 诊断/报告/服务/状态机相关定向 Node 测试。
- `node --test --experimental-strip-types test/*.test.ts`
- `node --experimental-strip-types scripts/doctor.ts`
- `node --experimental-strip-types src/cli/simulate.ts`
- `git diff --check` 和 `git status --short`（仅检查，不提交业务项目）。

## 验收标准

- 每个失败任务可回读失败阶段、脱敏原因、`changedFiles` 和下一步；180 秒无有效进度不再是无上下文的笼统错误。
- 安全任务可受控重试，危险/不确定/已取消/重复任务被明确拒绝且不产生副作用。
- 自检能发现模拟的轮询/TLS、Codex 和队列异常；受控恢复保留原任务状态与幂等语义。
- 定向/全量测试、doctor、simulate、Git 检查实际通过；真实外部链路未经单独授权测试时如实标记 `N/A`。

## 停止条件

- 需求扩大为真实飞书测试、批量重试、生产操作、数据库迁移、硬件或业务项目 Git 操作。
- 无法保存任务状态/幂等键/失败上下文，或重试可能导致未知外部副作用。
- 任一本地门禁失败、发现敏感数据泄露风险，或恢复会覆盖现有任务记录。

## 产出路径

- 当前计划：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/plan.md`
- 计划归档：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/产出/plan-2026-08-21.md`
- 当天协商结果：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/协商结果-2026-08-21.md`
- 目标项目：`/Users/jh-05/Documents/手机远程操控Codex`
