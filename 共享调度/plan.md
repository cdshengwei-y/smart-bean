# 2026-08-29 日清夜跑计划

## 唯一目标

继续收口录音豆 Windows/macOS 跨平台发布门禁，完成发布物事实核对与安装失败安全重跑验证，并明确正式客户包是否仍被外部证据阻断。

## 背景

- 今天未找到有效的 `协商结果-2026-08-29.md`，因此依据 8 月 28 日协商结果、项目交付检查表和当前发布脚本选择本目标。
- 项目已有 Windows 安装器、macOS 包、Obsidian 插件和 SHA-256 配置基础；交付记录仍明确缺少 Developer ID/公证、Windows EXE 签名、Defender/SmartScreen 与干净系统证据。
- 今日白天暂无项目代码、发布物或业务配置修改；计划用于夜间核验，不把计划写成已完成成果。

## 范围

1. 从实际 Windows/macOS 发布物核对版本、文件大小、SHA-256、插件资源和默认配置，形成可追溯清单。
2. 用本地 fixture/可控环境覆盖下载失败、哈希不一致、权限不足、Obsidian 未安装、重复执行和安全重跑，记录退出状态、清理结果和幂等行为。
3. 分离内部测试包与正式客户包门禁；缺 Developer ID、公证、Windows 签名、Defender/SmartScreen 或干净系统证据时阻止正式发布。
4. 由独立终审复核清单、失败路径、门禁状态和本轮差异边界。

## 非目标

- 不修改 Obsidian 本体或插件业务功能。
- 不伪造签名、公证、Defender/SmartScreen、干净客户系统或外部验收结果；缺证据记为 N/A/阻塞。
- 不连接生产服务，不操作真实用户数据或真实硬件。
- 不提交业务项目，不部署，不创建 PR，不迁移数据库，不修改线上配置。

## 执行者1-4分工

1. 执行者1：核对 Windows/macOS 实际发布物及清单字段，输出版本、大小、哈希、插件和配置证据。
2. 执行者2：验证下载失败、错误哈希、权限不足、Obsidian 缺失、重复执行和清理/重跑行为。
3. 执行者3：核对内部包/正式客户包门禁，整理签名、公证、Defender/SmartScreen 和干净系统证据缺口。
4. 执行者4：独立复跑可用测试和差异审计，裁定 PASS/PARTIAL/阻塞，不直接改共享文件。

## 依赖顺序

1. 读取项目发布脚本、发布清单、交付验收记录和上一轮回传。
2. 先完成实际发布物清单，再验证失败路径与安全重跑。
3. 以本地证据更新门禁状态，最后由执行者4独立终审。

## 约束

- SHA-256、大小和版本必须从实际产物计算，不以配置文本替代。
- 安装失败不得留下半安装状态、重复插件或覆盖用户已有 Vault 配置。
- 内部测试包可标注未签名/N/A；正式客户包缺任一签名、公证或干净系统证据必须阻断。
- 保留目标项目既有未提交修改；执行者只在会话回传，调度者统一落盘共享文件。

## 测试

- 发布物清单与哈希校验、插件/默认配置一致性检查。
- 安装器本地 fixture：下载失败、错误哈希、权限不足、Obsidian 缺失、重复执行、安全重跑。
- 按目标项目实际入口运行可用的定向/全量测试，并记录不可运行项为 N/A。
- `git diff --check` 与目标项目 `git status --short` 仅检查，不提交业务项目。

## 验收标准

- 清单与实际 Windows/macOS 包的版本、大小、SHA-256、插件和默认配置一致且可追溯。
- 失败路径和重复执行均有实际证据，失败可清理，安全重跑不破坏既有配置。
- 内部测试包与正式客户包门禁可区分；缺外部签名、公证或干净系统证据时正式发布明确阻断。
- 独立终审完成；所有测试、外部链路和 N/A/阻塞边界如实记录，不以构建成功替代发布通过。

## 停止条件

- 发现敏感信息进入客户包或发布清单。
- 任一失败路径留下不可恢复的半安装状态，或本地门禁失败。
- 外部签名、公证、Windows 环境、Defender/SmartScreen 或干净系统证据不可获得。
- 需求扩大到生产、真实客户数据、真实硬件或业务项目 Git 操作。

## 产出路径

- 当前计划：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/plan.md`
- 计划归档：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/产出/plan-2026-08-29.md`
- 协商结果：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/协商结果-2026-08-29.md`（未找到）
- 目标项目：`/Users/jh-05/Documents/smart recording beam`
