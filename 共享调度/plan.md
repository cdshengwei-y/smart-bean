# 2026-08-25 日清夜跑计划

## 唯一目标

统一 Windows/macOS 安装器使用 Obsidian 图标，并增加图标身份回归检查，确保安装器与内置插件使用同一可追溯图标资产。

## 背景

- 当天协商结果 `/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/协商结果-2026-08-25.md` 已记录用户确认目标“统一 Windows/macOS 安装器使用 Obsidian 图标，并增加图标身份回归检查”，本计划严格按该目标执行。
- 2026-08-24 本地发布门禁为 `PASS=25 / FAIL=0 / N/A=4`；安装器、Obsidian 插件和图标已有本地验证，但跨平台统一图标身份尚未形成独立回归合同。

## 范围

- 盘点 Windows/macOS 安装器、Obsidian 插件和现有部署脚本的图标来源、格式、路径和内容哈希。
- 以现有 Obsidian 图标资产为单一来源，最小修改 Windows/macOS 安装器的图标引用或打包配置。
- 增加离线回归检查，验证两端安装器和内置插件引用的图标身份一致，并复跑相关发布门禁。

## 非目标

- 不执行 Developer ID 签名、公证、Windows EXE 签名、Defender/SmartScreen、干净客户系统或真实网络下载验收。
- 不连接生产服务、不读取真实用户数据、不部署、不烧录、不操作硬件。
- 不提交或推送业务项目；仅提交共享计划材料。

## 执行者1-4分工

1. 执行者1：只读盘点跨平台图标资产和引用，输出差异与最小改动建议。
2. 执行者2：实现安装器图标引用统一与离线身份检查，回传修改文件和定向测试结果。
3. 执行者3：复跑 Windows/macOS 安装器、插件和部署门禁的相关本地测试，记录 PASS/FAIL/N/A。
4. 执行者4：独立审计图标来源、回归覆盖、实际测试与残余发布边界，仅回传不直接写共享文件。

## 依赖顺序

1. 先完成资产与引用盘点，确定现有 Obsidian 图标单一来源。
2. 基于盘点实施最小配置或资源修改，并加入离线回归。
3. 复跑定向和部署门禁，再由独立终审核验结果与未覆盖边界。

## 约束

- 不替换或删除现有用户文件；只修改图标引用及必要的离线检查。
- 回归不得通过网络下载、真实签名或客户环境结果伪造 PASS。
- 目标目录不是 Git 仓库时，如实记录，禁止虚构 Git 检查或提交。
- 执行者仅在任务会话回传，调度者统一落盘共享文件。

## 测试

- 新增或更新的图标身份定向测试。
- `PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest -v tests/test_windows_sync_assistant.py`。
- `python3 deployment/verify_deployment.py`；其中真实网络/签名/客户环境项目保持 N/A。

## 验收标准

- Windows/macOS 安装器与 Obsidian 插件的图标来源、格式和哈希可回读，差异有明确处理结果。
- 回归能在本地发现图标引用漂移或嵌入资源不一致。
- 定向测试与部署门禁的实际 PASS/FAIL/N/A 如实记录；签名、公证、Defender/SmartScreen、干净客户环境继续保持 N/A。

## 停止条件

- 需要真实发布证书、公证凭证、Windows 发布机、干净客户环境、生产服务、硬件或真实用户数据。
- 图标来源无法确认、任一本地门禁失败，或最小修改扩大为跨平台发布流程重构。

## 产出路径

- 当前计划：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/plan.md`
- 计划归档：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/产出/plan-2026-08-25.md`
- 协商结果：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/协商结果-2026-08-25.md`（已确认）
- 目标项目：`/Users/jh-05/Documents/smart recording beam`
