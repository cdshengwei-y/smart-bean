# 2026-08-20 日清夜跑计划

## 唯一目标

重新安排并验证一次真实受控只读任务闭环：使用不会触发夜跑状态查询的显式只读命令，确认任务入队后先返回“已接收，正在处理”，再按顺序产生阶段/最终回执，并核验机器人身份、目标群、回执顺序和 `changedFiles=[]`。若权限、常驻服务或真实链路条件不满足，严格记为 `PARTIAL/N/A`，不伪造 `PASS`。

## 背景与证据

- 今日协商结果文件 `/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/协商结果-2026-08-20.md` 未找到，故沿用上一轮明确遗留的唯一目标，不宣称已完成协商。
- 2026-08-19 真实测试消息被旧解析规则误判为夜跑状态查询，未创建任务；真实任务回执链和 `changedFiles=[]` 尚无证据。上一轮最终状态为 `PARTIAL`。
- 目标项目 `/Users/jh-05/Documents/手机远程操控Codex` 今日本地门禁已核验：全量测试 `82/82` 通过，`doctor` 退出码 0；这些本地结果不等同于真实飞书链路通过。
- 目标项目存在用户未提交修改，必须保留；本轮不提交或推送业务项目。

## 范围

- 复核正确“叶聪宇”受控群、用户与机器人身份、成员关系、服务运行态和重复发送风险。
- 选择不含“夜跑/报告”状态查询关键词的显式只读命令，且预期无文件变更。
- 在任务创建后回读受理回执、阶段回执和最终回执，记录消息 ID、发送身份、群 ID、时间顺序与 `changedFiles`。
- 复跑与本目标相关的本地测试、doctor、simulate 和 `git diff --check`，如实汇总 `PASS/PARTIAL/FAIL`。

## 非目标

- 不压力测试，不发送第二条重复真实测试消息，不读取私聊、日历、真实用户数据或生产服务。
- 不修改产品方向，不绕过鉴权、幂等、只读隔离或终态门禁。
- 不提交/推送业务项目，不部署、不烧录、不连接真实硬件或生产服务。

## 执行者 1-4 分工

1. 执行者1：核验受控群、用户/机器人身份、成员关系、常驻服务和只读边界；不发送测试消息。
2. 执行者2：核验受理回执精确正文、落库/唤醒时序及失败不丢任务语义。
3. 执行者3：执行一次真实受控只读命令，回读受理、阶段、最终回执和 `changedFiles=[]`；条件不满足即停止并记 `N/A/阻塞`。
4. 执行者4：独立复跑本地门禁，审计身份、群 ID、消息顺序、重复风险和状态结论；不直接写共享文件。

## 依赖顺序

1. 先完成身份、群、权限、服务和只读边界核验。
2. 受理回执合同核验通过后，才允许执行一次真实命令。
3. 真实回执证据齐全后由执行者4独立终审；任一硬门禁失败立即停止。
4. 调度者统一落盘计划、回传、产出和表格记录。

## 约束

- 受理正文必须是“已接收，正在处理”；最终回执不得冒充阶段回执。
- 真实测试只允许目标群一次；群 ID、发送者、机器人身份、消息顺序或 `changedFiles` 任一不符即停止。
- 所有测试数字注明命令和环境；本地 PASS 与真实外部链路分开记录。
- 只读测试不得产生业务文件改动；保留目标项目现有未提交修改。

## 测试

- `node --test --experimental-strip-types test/*.test.ts`
- `node --experimental-strip-types scripts/doctor.ts`
- `node --experimental-strip-types src/cli/simulate.ts`
- `git diff --check`（仅检查，不提交业务项目）
- 真实回读：按消息 ID核对正文、sender_type、发送者、群 ID、顺序、时间戳和 `changedFiles=[]`。

## 验收标准

- 真实任务确实创建；受理回执先于阶段/最终回执，且正文、身份和群 ID正确。
- 最终回执明确只读结果，`changedFiles=[]`，没有重复或越权消息。
- 本地门禁结果与真实链路结果分别记录；真实链路缺证据时整轮最高为 `PARTIAL`。

## 停止条件

- 群、身份、权限、常驻服务或只读边界无法核验。
- 解析再次被夜跑状态查询抢占、出现错误群/身份/重复消息、受理或最终回执顺序错误。
- `changedFiles` 非空、任一本地门禁失败，或需求扩大到生产、部署、硬件或业务项目 Git 操作。

## 产出路径

- 当前计划：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/plan.md`
- 计划归档：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/产出/plan-2026-08-20.md`
- 目标项目：`/Users/jh-05/Documents/手机远程操控Codex`
- 材料仓库：`ssh://git@ssh.github.com:443/cdshengwei-y/smart-bean.git`
