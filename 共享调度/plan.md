# 2026-09-09 日清夜跑计划

## 唯一目标与背景

依据 `协商结果-2026-09-09.md`，合并完成声呐 V0.2 的三项收口工作：修正 30 m/5 mm 模拟测距标定误差；将 V0.2 实验报告、参数、测试结果和限制沉淀到声呐知识库；补齐 STM32 串口/CSV 数据合同，并用本地 fixture 验证“采样数据输入—测距—绘图”接口。

上一轮本地模拟闭环已输出 CSV、JSON、波形和二维/三维图，正常回波、无回波、噪声、多回波回归通过；但 30 m 临时结果为 29.985072 m，误差 14.928 mm，超过 5 mm 容差，整体为 PARTIAL。今天只在本地模拟和文档/接口范围内收口。

## 范围

1. 复现 30 m 边界误差，定位采样率、时间零点、声速/温度或量化换算中的标定偏差。
2. 以版本化前后参数和结果证据修正模拟标定，使 30 m 误差满足约定的 5 mm 容差；若无法达到，保留失败原因并标记 PARTIAL。
3. 整理 V0.2 实验报告、参数、四类测试结果、CSV/JSON/图表、限制项和前后对比，复制或链接到 `/Users/jh-05/Documents/声呐` 知识库，保留来源和去重记录。
4. 定义 STM32 串口/CSV 数据合同：采样率、时间戳单位、距离/深度单位、温度单位、状态枚举、空值与版本字段；提供代表性 fixture。
5. 用 fixture 验证采样数据输入、测距输出、CSV 解析和基础二维/三维绘图接口，保存命令、结果和证据到 `产出/2026-09-09-sonar-v02-closeout/`。

## 非目标

- 不把模拟结果当作真实硬件验收，不承诺真实 30 m 量程。
- 不进行真实换能器、TUSS4470、STM32 接线、供电、烧录、水下测试或实地地形采集，因此不新增实板人工验收任务；相关项明确标记 N/A。
- 不做生产部署、业务项目 Git 提交/推送、PR、数据库迁移、线上配置修改或真实用户数据操作。
- 不引入复杂地图算法，不覆盖未定义的生产协议兼容性。

## 执行者 1-4 分工

1. 执行者1：复现 30 m 误差，核对采样时钟、ToF 起点、声速/温度和量化参数，提出最小标定修正及前后对比。
2. 执行者2：实现修正并回归正常、无回波、噪声、多回波及 30 m/5 mm 边界，记录 PASS/PARTIAL 和失败证据。
3. 执行者3：整理实验报告、参数、测试产物和限制，更新声呐知识库索引与来源说明；定义串口/CSV 合同和本地 fixture。
4. 执行者4：独立运行 fixture 和关键回归，核验合同字段、图表可读性、知识库可追溯性，裁定整体 PASS/PARTIAL。

## 依赖顺序

先复现并锁定 30 m 误差基线，再修改标定并跑完整回归；随后冻结参数和结果，整理知识库；最后按冻结合同生成 fixture，验证输入—测距—绘图接口并由执行者4复核。任何修正必须保留基线与新结果，不能覆盖失败证据。

## 约束

- 仅使用已授权的声呐方案、昨日共享产物、`/Users/jh-05/Documents/声呐` 知识库和本地项目状态；不读取私聊、日历、生产服务或真实用户数据。
- 不修改业务项目源码；允许在共享调度产出目录生成脚本、fixture、报告和测试证据，知识库导入保留来源、去重且不覆盖已有资料。
- 串口/CSV 合同必须写清版本、字段顺序、单位、状态枚举、空值和异常处理；未知字段不得猜测。
- 所有测试、人工验收、N/A 和阻塞如实记录；模拟通过不等于固件或实板通过。

## 测试

- 30 m/5 mm：复现旧基线，验证新标定绝对误差是否不超过 0.005 m，并保留前后 JSON/文本结果。
- 四类回归：正常回波、无回波、噪声、多回波，核对状态、深度和重复运行一致性。
- 合同解析：fixture 覆盖完整行、无距离空值、未知状态、缺列/坏数值和版本字段；检查错误是否可诊断。
- 端到端：fixture 输入可完成采样数据解析、ToF/距离输出、CSV 读回和基础二维/三维绘图。
- 知识库：报告、参数、原始结果、图表和限制可打开，索引/来源/去重记录完整；人工 Obsidian 搜索若未执行标 N/A。

## 验收标准

- 30 m 模拟边界达到 5 mm 容差，或有可复核的失败证据、明确 PARTIAL 原因和下一步。
- 声呐知识库可追溯展示 V0.2 报告、参数、测试结果、图表和限制，未覆盖真实硬件结论。
- 串口/CSV 合同和 fixture 可独立读取，字段、单位、状态、错误处理与版本明确。
- 本地端到端 fixture 回归通过，输出仍包含 `timestamp_ms,depth_m,temperature_c,status`，图形可读。
- 真实硬件接入、固件烧录、供电和水下验收均为 N/A，不得写成 PASS。

## 停止条件

- 需要真实硬件接线、供电、烧录、水下测试、生产服务或未授权资料。
- 标定修正改变已验证场景但无法解释，或前后结果不可复现、证据丢失。
- 合同字段/单位/版本无法由现有方案确认，继续会造成固件接口歧义。
- 知识库导入出现敏感资料、递归复制、同名覆盖或来源不可追溯。

## 产出路径

- 当前计划：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/plan.md`
- 当天归档：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/产出/plan-2026-09-09.md`
- 收口证据：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/产出/2026-09-09-sonar-v02-closeout/`
- 昨日基线证据：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/产出/2026-09-08-sonar-v02/`
- 声呐知识库：`/Users/jh-05/Documents/声呐/`
- 协商结果：`/Users/jh-05/Documents/ChatGPT/日清夜跑执行者/共享调度/协商结果-2026-09-09.md`
